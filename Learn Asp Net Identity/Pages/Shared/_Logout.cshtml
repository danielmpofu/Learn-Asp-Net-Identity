@model TModel

@if (User.Identity.IsAuthenticated)
{
    <form method="post" class="form-inline" asp-page="/Account/Logout">
         Welcome @User.Identity.Name
         <button class="btn "></button>
    </form>
}
else
{
    <a class="btn btn-link" asp-page="/Account/Login"></a>
}