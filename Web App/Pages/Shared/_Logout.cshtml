@if (User.Identity.IsAuthenticated)
{
    <form method="post" class="form-inline" asp-page="/Account/SignOut">
        Welcome @User.Identity.Name
        <button class="btn ml-2"> Logout</button>
    </form>
}
else
{
    <a class="btn btn-link" asp-page="/Account/Login"></a>
}